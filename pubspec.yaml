name: letsgo
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  http: ^1.6.0
  flutter_typeahead: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Section des Assets regroupée ici
  assets:
    - assets/map_capture.png
    # Ajoute tes autres images ici si besoin (ex: - assets/logo.png)
